import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'pwa-getting-started',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
